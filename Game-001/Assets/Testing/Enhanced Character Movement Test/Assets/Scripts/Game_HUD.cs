using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using System;

public class Game_HUD : MonoBehaviour {
    /*
    Controller2D controller;
    Animator animator;
    public Player player;
    public Text facingDirection;
    public Text onTheGround;
    public Text onASlope;
    public Text colAbove;
    public Text yValue;

    string d;
    bool onSlope;

    void Start()
    {
        controller = player.GetComponent<Controller2D>();
        animator = player.GetComponent<Animator>();
    }


    void Update()
    {

        UIUpdate();


    }




    void UIUpdate()
    {
        if (controller.collisions.faceDir == 1) { d = ("Right"); }
        if (controller.collisions.faceDir == 0) { d = ("None"); }
        if (controller.collisions.faceDir == -1) { d = ("Left"); }
        facingDirection.text = d;

        onTheGround.text = controller.collisions.below.ToString();

        if ((controller.collisions.climbingSlope) || (controller.collisions.descendingSlope))
        {
            onSlope = true;
        }
        else
        {
            onSlope = false;
        }

        onASlope.text = onSlope.ToString();

        colAbove.text = controller.collisions.above.ToString();


       // yValue.text = (Math.Sign(player.velocity.y)).ToString();
    }*/

}

